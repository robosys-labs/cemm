# FROZEN — Permanent Reference

**Status:** Frozen. Do not modify.
**Purpose:** Permanent executable reference of the CEMM MVP v2 demo.

This directory is a read-only reference. CEMM v1 is built from mvp_v4 but lives
in the root `cemm/` package. To run this reference:

```bash
cd reference/mvp_v2
python -m unittest -v
```

**Key contribution:** Minimal 5-operator kernel (489 lines), ephemeral closure,
learned codec, inverse round-trip verification.
