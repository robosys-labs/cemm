# Phase-by-phase completion plan

## Phase 0 — authority lock

- Pin commit and baseline traces.
- Freeze the `newarch` documents as the contract.
- Add forbidden-pattern scanner.
- No behavior change.

Exit gate: baseline is reproducible and every active authority is listed.

## Phase 1 — executable foundations

- Install foundation predicate contracts.
- Install audited ordinary boot schema contracts.
- Replace `v341.py` with a data-only loader.
- Fail boot for missing implementations/property cases.

Exit gate: no domain concept exists in foundation data; boot validation passes.

## Phase 2 — goals, requirements, and self claims

- Add `GoalRecord`, `EvidenceSpecification`, `GoalDependency`, and
  `RequirementAssessment`.
- Derive requirements only from live unsatisfied dependencies.
- Install semantic self-claim policies.

Exit gate: “self requires X” is impossible without a matching live assessment.

## Phase 3 — semantic emission closure

- Add proof records and emission gate.
- Replace content-kind renderer switches.
- Add complete surface-span coverage.
- Restrict literals to punctuation and spacing.

Exit gate: deleting any foundation, lexical, grammatical, self-state, or
round-trip proof blocks the clause.

## Phase 4 — reversible multilingual understanding

- Install typed input construction AST.
- Load English and French input packs through one matcher.
- Preserve raw form, offsets, alternatives, and opaque mentions.

Exit gate: parallel language inputs can produce equivalent canonical graph
shapes without language conditions in kernel code.

## Phase 5 — grounded recursive learning

- Compile all schema families.
- Require operational attachment and grounded dependencies.
- Add independent competence provenance.
- Replay and activate through the ordinary store.

Exit gate: a parent-label lesson remains staged; an independently competent,
operationally attached artifact becomes structurally executable.

## Phase 6 — bounded semantic inference

- Install proof-carrying rule records and agenda engine.
- Add existential constraints, exceptions, sensitivity, warrant, and budgets.
- Classify recursive components.

Exit gate: kinship examples do not invent people; defaults remain defeasible;
sensitive associations remain disabled.

## Phase 7 — journal, commit, and core-loop cutover

- Journal semantic mutations and proofs atomically.
- Derive requirements in KNOW.
- Run bounded inference/learning in ACT AND RECONCILE.
- Run emission closure in COMMUNICATE.
- Commit discourse only after dispatch.

Exit gate: trace and store revisions demonstrate the complete authority path.

## Phase 8 — legacy removal and acceptance

- Remove phrase-specific English code and old boot tables.
- Delete or rewrite tests that assert domain words are foundations.
- Run multilingual, learning, inference, persistence, and NLG acceptance suites.
- Make any legacy authority import fail CI.

Exit gate: no legacy authority can answer a production turn.
