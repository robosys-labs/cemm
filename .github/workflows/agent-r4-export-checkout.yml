name: Agent R4 export checkout

on:
  push:
    branches:
      - agent/r4-export-checkout-20260816
    paths:
      - .github/workflows/agent-r4-export-checkout.yml

permissions:
  contents: read

concurrency:
  group: agent-r4-export-checkout-20260816
  cancel-in-progress: false

jobs:
  export:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/r4-task4-publish-20260816
          fetch-depth: 0
          persist-credentials: false
      - name: Package alternate Task 4 source envelope
        run: |
          set -euo pipefail
          tar --exclude=.git -czf /tmp/cemm-r4-task4-source-envelope.tgz .
          sha256sum /tmp/cemm-r4-task4-source-envelope.tgz > /tmp/cemm-r4-task4-source-envelope.sha256
      - name: Upload source envelope archive
        uses: actions/upload-artifact@v4
        with:
          name: cemm-r4-task4-source-envelope
          retention-days: 1
          if-no-files-found: error
          path: |
            /tmp/cemm-r4-task4-source-envelope.tgz
            /tmp/cemm-r4-task4-source-envelope.sha256
