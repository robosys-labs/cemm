name: Agent R4 Task 6 bootstrap
on:
  push:
    branches:
      - agent/r4-task6-build-receipt-20260818
permissions:
  contents: read
jobs:
  smoke:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - run: git status --short
